<div class="row">
  <div class="logo-container"></div>

  <div class="header-items">
    <div class="header-items-cell">
      {{#each _embedded.headerItems as |headerItem|}}
        <span class="header-item">
          {{#if headerItem.isCurrentPage}}
            <span class="current-page">{{headerItem.label}}</span>
          {{else if headerItem._links.self.href}}
            <a href="{{headerItem._links.self.href}}">{{headerItem.label}}</a>
          {{else}}
            {{headerItem.label}}
          {{/if}}
        </span>
      {{/each}}
    </div>

    {{>menu menuItems=_embedded.headerMenuItems}}
  </div>
</div>
